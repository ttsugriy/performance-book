name: Build and Deploy Book

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jupyter matplotlib numpy scipy pandas

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install TinyTeX
        run: quarto install tinytex

      - name: Install additional LaTeX packages
        run: |
          ~/.TinyTeX/bin/*/tlmgr install fvextra framed upquote lineno fancyvrb etoolbox xcolor || true

      - name: Render HTML
        run: quarto render --to html

      - name: Render PDF
        timeout-minutes: 20
        run: quarto render --to pdf
        continue-on-error: true

      - name: Copy PDF to site
        run: |
          if [ -f "_book/The-Algebra-of-Speed.pdf" ]; then
            cp "_book/The-Algebra-of-Speed.pdf" "_site/The-Algebra-of-Speed.pdf"
            echo "PDF copied from _book/"
          elif [ -f "The-Algebra-of-Speed.pdf" ]; then
            cp "The-Algebra-of-Speed.pdf" "_site/The-Algebra-of-Speed.pdf"
            echo "PDF copied from root"
          else
            echo "No PDF found, skipping copy"
          fi

      - name: Setup Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v4

      - name: Upload artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
